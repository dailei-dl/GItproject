# Value Engine API

## Demo Flow

```http
POST /value-engine/demo-flow
```

Returns rule version `VALUE-RULE-MVP-1`, project value pool, department allocation, personal allocation, adjustment status, and calculation trace.

The engine uses the shared money-safe formula helpers from `@designtwin/shared`.

